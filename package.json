{
  "name": "greedy-workspace",
  "private": true,
  "workspaces": [
    "greedy"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=greedy",
    "build": "npm run build --workspace=greedy",
    "start": "npm run start --workspace=greedy",
    "lint": "npm run lint --workspace=greedy"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "dependencies": {
    "sharp": "^0.34.4",
    "sonner": "^2.0.7"
  }
}
